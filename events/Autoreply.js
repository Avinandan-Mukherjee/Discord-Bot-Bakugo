const Discord = require("discord.js");

module.exports = {
  
  run: async (shooter, message, args) => {const Discord = require("discord.js");

    if(message.content === 'hi') return message.reply({content: "hello"})
  }
}