
module.exports = {
  name: "pause",
  description: "Pause a song (Currently disabled)",

  run: async (shooter, message, args) => {
    message.reply("Music commands are currently disabled.");
  }
}
